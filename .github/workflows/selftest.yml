name: Selftest

on: [push, pull_request]

jobs:
    build:
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os:
                    - macos-12
                    - macos-14
                    - macos-latest
                    - ubuntu-latest
                    - windows-latest

        steps:
          - name: Checkout repository
            uses: actions/checkout@v4

          - name: Run action
            uses: ./